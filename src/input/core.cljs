(ns input.core
  "This provides the various input functions which provide input for the engine.
  It also serves to preprocess the input wherever possible to produce values
  that use engine units instead of render units.")
